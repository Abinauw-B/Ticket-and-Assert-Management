services:
  - type: web
    name: ticket-asset-backend
    rootDir: backend
    env: java
    region: oregon # or frankfurt, ohio, singapore
    buildCommand: ./gradlew build
    startCommand: java -jar build/libs/ticket-asset-management-0.0.1-SNAPSHOT.jar
    plan: free
